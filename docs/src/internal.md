These are some functions, which are in general not usefull to a person using the package:

```@docs
    FGVertexCandidate

    is_isomorphic(::FGVertexCandidate, ::FGVertex)
    rename_vertices!
    diameter(::FGVertex)
```